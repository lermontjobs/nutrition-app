[build]
builder = "nixpacks"
buildCommand = "npm install && npx prisma generate && npx prisma migrate deploy && node scripts/seed-prod.js && npm run build"

[deploy]
startCommand = "npm start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
